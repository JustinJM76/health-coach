# Secrets — Withings OAuth credentials and tokens
.env
.env.local

# OS / editor noise
.DS_Store
.vscode/
.idea/

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.python-version
venv/
.venv/
env/

# Logs / temp
*.log
*.tmp
.cache/

# Generated dashboard HTML — regenerated by cron after each plan write
dashboard/*.html
dashboard/__pycache__/

# Local Claude runtime state — but keep project-scoped skills tracked.
# Use /* (not /) so negation rules below can re-include skills.
.claude/*
!.claude/skills/
